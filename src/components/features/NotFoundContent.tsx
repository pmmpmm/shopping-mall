import MessageContent from "@/components/features/MessageContent";

const NotFoundContent = () => {
  return <MessageContent type="notFound" height={"h-[100vh]"} />;
};

export default NotFoundContent;
